import React, { useState } from 'react';
import VendorNavbar from '../VendorNavbar';

const AddItems = () => {
    return (
        <div>
            <VendorNavbar />
            <p>This is the screen to add items!</p>
        </div>
    )
}

export default AddItems;